apiVersion: v1
kind: Service
metadata:
  namespace: kearch
  name: sp-front
spec:
  type: NodePort
  ports:
  - port: 2222
    targetPort: 2222
    protocol: TCP
  selector:
    engine: sp
    app: front
  externalIPs:
  # - <set your external global ip here>
