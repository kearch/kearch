apiVersion: v1
kind: Service
metadata:
  name: sp-es
  labels:
    engine: sp
    component: elasticsearch
    role: data
spec:
  selector:
    component: elasticsearch
    role: data
  type: NodePort
  ports:
  - name: http
    port: 9200
#type: LoadBalancer
